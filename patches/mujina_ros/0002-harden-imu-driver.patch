diff --git a/rt_usb_imu_driver/src/parser.cpp b/rt_usb_imu_driver/src/parser.cpp
index 96996db..0174d1f 100644
--- a/rt_usb_imu_driver/src/parser.cpp
+++ b/rt_usb_imu_driver/src/parser.cpp
@@ -49,7 +49,6 @@ bool Parser::parse() {
     if (end == std::string::npos)
       break;
     std::string line = serial_data_.substr(start, end - start);
-    std::cout << "Parse line: " << line << std::endl;
     std::vector<double> values;
     bool has_nonfinite = false;
     size_t pos = 0;
@@ -78,8 +77,7 @@ bool Parser::parse() {
       latest_data_ = values;
       update_flag = true;
     } else {
-      std::cout << "Warning: Expected 8 values, got " << values.size()
-                << std::endl;
+      // Ignore malformed frames; live health checks catch stale data.
     }
     start = end + 1;
   }
diff --git a/rt_usb_imu_driver/src/rt_usb_imu_driver.cpp b/rt_usb_imu_driver/src/rt_usb_imu_driver.cpp
index 8b2e4ef..f4c6770 100644
--- a/rt_usb_imu_driver/src/rt_usb_imu_driver.cpp
+++ b/rt_usb_imu_driver/src/rt_usb_imu_driver.cpp
@@ -39,6 +39,7 @@
 #include <iomanip> // std::setw(int), std::setfill(char)
 #include <iostream>
 #include <memory>
+#include <stdexcept>
 #include <string>
 
 using namespace std::chrono_literals;
@@ -47,11 +48,12 @@ class RtUsbImuDriverNode : public rclcpp::Node {
 public:
   RtUsbImuDriverNode() : Node("rt_usb_imu_driver") {
     this->declare_parameter("port_name", "/dev/ttyUSB0");
+    this->declare_parameter("baud_rate", 921600);
     std::string port_name = this->get_parameter("port_name").as_string();
-    // open port
-    bool res = openPort(port_name);
+    int baud_rate = this->get_parameter("baud_rate").as_int();
+    bool res = openPort(port_name, baud_rate);
     if (!res) {
-      // open失敗時処理
+      throw std::runtime_error("failed to open IMU serial port");
     }
     clearPort();
     // Normally you wouldn't do this memset() call, but since we will just
@@ -63,7 +65,11 @@ public:
     data_parse_timer_ = this->create_wall_timer(
         1ms, std::bind(&RtUsbImuDriverNode::data_parse_callback, this));
   }
-  ~RtUsbImuDriverNode() { close(port_fd_); }
+  ~RtUsbImuDriverNode() {
+    if (port_fd_ >= 0) {
+      close(port_fd_);
+    }
+  }
 
 private:
   void data_parse_callback() {
@@ -107,7 +113,7 @@ private:
     uint32_t failsafe;
     uint8_t dummy[MAX_PACKET_LEN];
 
-    if (port_fd_) {
+    if (port_fd_ >= 0) {
       for (failsafe = 0; failsafe < 0x7FFFFFFF; ++failsafe) {
         buff_num = 0;
         ioctl(port_fd_, TIOCINQ, &buff_num);
@@ -130,7 +136,7 @@ private:
     return;
   }
 
-  bool openPort(std::string &port_name) {
+  bool openPort(std::string &port_name, int baud_rate) {
     port_fd_ = open(port_name.c_str(), O_RDWR | O_NOCTTY | O_NONBLOCK);
 
     if (port_fd_ < 0) {
@@ -138,7 +144,8 @@ private:
                   port_name.c_str());
       return false;
     } else {
-      struct termios term;
+      struct termios term {};
+      speed_t speed = baud_rate == 921600 ? B921600 : B38400;
       term.c_cflag &= ~PARENB; // Clear parity bit, disabling parity
       term.c_cflag &=
           ~CSTOPB; // Clear stop field, only one stop bit used in communication
@@ -155,8 +162,8 @@ private:
       term.c_cc[VTIME] = 0; //
       term.c_cc[VMIN] = 0;
 
-      cfsetispeed(&term, B38400);
-      cfsetospeed(&term, B38400);
+      cfsetispeed(&term, speed);
+      cfsetospeed(&term, speed);
 
       if (tcsetattr(port_fd_, TCSANOW, &term) != 0) {
         RCLCPP_WARN(this->get_logger(), "Error %i from tcsetattr: %s\n", errno,
@@ -178,7 +185,7 @@ private:
     size_t buff_num;
     int32_t rx_res = 0;
 
-    if (port_fd_) {
+    if (port_fd_ >= 0) {
       buff_num = 0;
       ioctl(port_fd_, TIOCINQ, &buff_num);
       if (buff_num > 0) {
@@ -199,7 +206,7 @@ private:
   rt_usb_imu_data_parser::Parser p_;
 
   static const int MAX_PACKET_LEN = 256;
-  int port_fd_;
+  int port_fd_ = -1;
   // Allocate memory for read buffer, set size according to your needs
   char read_buf_[MAX_PACKET_LEN];
 };
